(() => {
  'use strict';
  const HQ = window.FranchiseHQ;
  if (!HQ?.defineModuleService) throw new Error('platform/core.js must load before companion-import-ui.js.');
  const VERSION='5.9.1.1';
  const escapeHtml=(value)=>String(value??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
  let busy=false;
  let message='Select a Madden Companion JSON file or load the sample payload.';

  function mapper(){return HQ.leagueCompanionJsonMapper;}
  function renderIssues(items,tone){return items.length?`<div class="companion-preview-issues companion-preview-issues--${tone}">${items.slice(0,8).map(item=>`<div><strong>${escapeHtml(item.code)}</strong><span>${escapeHtml(item.message)}</span></div>`).join('')}${items.length>8?`<small>Plus ${items.length-8} additional ${tone}${items.length-8===1?'':'s'}.</small>`:''}</div>`:'';}
  function renderPanel(){
    const preview=mapper()?.getLastPreview?.();
    const result=preview?`<div class="companion-preview-summary"><div><span>Contract</span><strong>${escapeHtml(preview.mapped.contract.version)}</strong></div><div><span>Season</span><strong>${preview.mapped.metadata.season??'Missing'}</strong></div><div><span>Week</span><strong>${preview.mapped.metadata.week??'Missing'}</strong></div><div><span>Teams Mapped</span><strong>${preview.mapped.teams.length}</strong></div><div><span>Players Mapped</span><strong>${preview.mapped.players.length}</strong></div><div><span>Activation</span><strong>Not performed</strong></div></div>${renderIssues(preview.validation.errors,'errors')}${renderIssues(preview.validation.warnings,'warnings')}`:'<div class="import-framework-empty">No JSON payload has been previewed.</div>';
    return `<article class="card companion-json-import-card" data-companion-import-panel><div class="card-header"><div><span class="eyebrow">v5.9.1.1 · Preview only</span><h3>Companion JSON Payload Mapper</h3><p>Choose a JSON export to parse, normalize, and inspect before any snapshot is created or activated.</p></div><span class="pill pill--${preview?.validation.valid?'success':preview?'warning':'neutral'}">${preview?preview.validation.valid?'Ready for Teams Import':'Needs attention':'Awaiting JSON'}</span></div><div class="companion-json-controls"><label class="companion-file-field"><span>Companion JSON file</span><input type="file" accept="application/json,.json" data-companion-json-file ${busy?'disabled':''}></label><div class="companion-json-actions"><button class="button button--primary" data-companion-preview-file ${busy?'disabled':''}>Preview Selected File</button><button class="button button--ghost" data-companion-load-sample ${busy?'disabled':''}>Load Sample Payload</button><button class="button button--ghost" data-companion-clear-preview>Clear Preview</button></div></div><p class="companion-json-message">${escapeHtml(message)}</p>${result}<div class="league-import-framework-note"><svg><use href="#icon-info"></use></svg><span>This story never changes the active snapshot. Team activation begins in v5.9.1.2.</span></div></article>`;
  }
  function renderMounted(){document.querySelectorAll('[data-companion-import-panel]').forEach(el=>{el.outerHTML=renderPanel();});}
  async function previewSelected(){const input=document.querySelector('[data-companion-json-file]');const file=input?.files?.[0];if(!file)throw new Error('Select a JSON file before previewing.');busy=true;message=`Reading ${file.name}…`;renderMounted();try{const result=await mapper().preview(file,{filename:file.name});message=result.validation.valid?`${file.name} mapped successfully. No snapshot was activated.`:`${file.name} was parsed, but the contract has blocking errors.`;return result;}finally{busy=false;renderMounted();}}
  async function loadSample(){busy=true;message='Mapping the built-in Companion sample…';renderMounted();try{const result=await mapper().preview(mapper().samplePayload(),{filename:'sample-companion-payload.json'});message='Sample payload mapped successfully. No snapshot was activated.';return result;}finally{busy=false;renderMounted();}}
  function clear(){mapper()?.clearPreview?.();message='Preview cleared. Active league data was not changed.';renderMounted();}
  document.addEventListener('click',async event=>{const button=event.target.closest('[data-companion-preview-file],[data-companion-load-sample],[data-companion-clear-preview]');if(!button)return;event.preventDefault();try{if(button.hasAttribute('data-companion-preview-file'))await previewSelected();else if(button.hasAttribute('data-companion-load-sample'))await loadSample();else clear();}catch(error){busy=false;message=error.message;console.error('[Companion JSON Preview]',error);renderMounted();}},true);
  HQ.defineModuleService('league','leagueCompanionImportUI',{renderPanel,renderMounted,previewSelected,loadSample,clear,diagnostics:()=>Object.freeze({service:'leagueCompanionImportUI',version:VERSION,busy,previewOnly:true})},{replace:true,alias:'leagueCompanionImportUI'});
  HQ.manifest?.register?.({scope:'module',module:'league',id:'league-companion-import-ui',service:'leagueCompanionImportUI',script:'league-engine/companion-import-ui.js',version:VERSION,dependencies:['leagueCompanionJsonMapper'],capabilities:['json-file-selection','mapping-preview','sample-payload','no-activation-guard']});
})();
