# Franchise HQ v5.5.1a — Roster Experience Startup Hotfix

Corrects script order so `app.js` creates `window.FGC_APP` before `platform/domains.js` initializes. No roster UI or read-model behavior changes.
